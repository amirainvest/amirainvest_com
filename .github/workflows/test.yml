name: Test
on:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: "Test"
    runs-on: ubuntu-latest
    steps:

      - name: Test
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run:  echo "$GITHUB_CONTEXT"
